# Local Development

```bash
npm install
npm run typecheck
npm test
npm run build
node dist/cli.js doctor --verbose
```

User releases should be standalone macOS binaries. Package manager usage is for contributors only.
